# Per-repo merge-gate thresholds. A repo merges only when its blended eval
# score (mean of gated metrics) meets or exceeds its threshold.
default_threshold: 0.76
# Repos without an entry inherit default_threshold.
repos:
  retrieval-core:
    threshold: 0.80

  agentflow:
    threshold: 0.80

  graphmind:
    threshold: 0.77

  llmjudge:
    threshold: 0.84

  shipwright:
    threshold: 0.86
