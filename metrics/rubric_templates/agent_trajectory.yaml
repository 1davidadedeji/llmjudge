metric: agent_trajectory
version: 1
criteria:
  - "Expected tools must be called, in the expected relative order."
  - "Extra steps reduce the efficiency component of the score."
scale_min: 0
scale_max: 1
